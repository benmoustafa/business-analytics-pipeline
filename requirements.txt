# Ingestion
kaggle>=1.6
pandas>=2.0
pyarrow>=14.0

# Warehouse loading
sqlalchemy>=2.0
psycopg2-binary>=2.9

# dbt (optional — only needed if running dbt locally outside Docker)
# dbt-core>=1.7
# dbt-postgres>=1.7
